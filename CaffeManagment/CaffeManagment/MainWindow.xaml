<Controls:MetroWindow x:Class="CaffeManagment.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:local="clr-namespace:CaffeManagment.View"
                      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                      xmlns:viewModel="clr-namespace:CaffeManagment.ViewModel"
                      xmlns:views="clr-namespace:CaffeManagment.View"
                      xmlns:data="clr-namespace:CaffeManagment.Model"
                      Title="Caffe"
                      Height="600"
                      Width="800"
                      Icon="pack://application:,,,/CaffeManagment;component/Resources/Images/logo.jpg">

    <Window.DataContext>
        <viewModel:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:TablesViewModel}">
            <views:TablesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:PriceListViewModel}">
            <views:PriceListView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.35*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Image Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Source="pack://application:,,,/CaffeManagment;component/Resources/Images/logo.jpg" />
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Vertical" Background="CornflowerBlue" x:Name="StackPanelMenu" Width="100" HorizontalAlignment="Left" Margin="0 0 0 0" RenderTransformOrigin="0.5,0.5">
            <ListView x:Name="listView">
                <ListViewItem x:Name="listViewItem" Height="60" RenderTransformOrigin="0.5,0.5" Selected="TablesCall">
                    <StackPanel Orientation="Horizontal" Margin="5 0">
                        <TextBlock Text="NALOG" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListViewItem>
                <ListViewItem x:Name="listViewItem1" Height="60" RenderTransformOrigin="0.5,0.5" Selected="PriceListCall">
                    <StackPanel Orientation="Horizontal" Margin="5 0">
                        <TextBlock Text="CENOVNIK" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListViewItem>
            </ListView>
        </StackPanel>

        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding ViewModelTitle}" Foreground="#FF3580BF" FontSize="20" FontWeight="Bold" Margin="5" Grid.Row="0"/>
            <Grid Grid.Row="1" Grid.Column="0">
                <ContentControl Content="{Binding CurrentViewModel}"/>
            </Grid>
        </Grid>
    </Grid>
</Controls:MetroWindow>
